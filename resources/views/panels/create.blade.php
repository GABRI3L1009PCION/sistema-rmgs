@extends('layouts.app')
@section('content')
    <section class="card"><div class="card-title"><h2>Registrar nuevo modelo de panel</h2><i data-lucide="grid-2x2"></i></div><p class="muted">Agrega un modelo al catalogo para poder asignarlo a las granjas solares.</p><form method="post" action="{{ route('panels.store') }}" class="section">@csrf<div class="form-grid"><label>Marca<input name="brand" value="{{ old('brand') }}" required>@error('brand')<span class="error">{{ $message }}</span>@enderror</label><label>Modelo<input name="model" value="{{ old('model') }}" required>@error('model')<span class="error">{{ $message }}</span>@enderror</label><label>Potencia nominal (kW)<input name="nominal_power_kw" type="number" step="0.001" min="0.001" value="{{ old('nominal_power_kw','0.450') }}" required>@error('nominal_power_kw')<span class="error">{{ $message }}</span>@enderror</label><label>Estado<select name="status" required><option value="active">Activo</option><option value="inactive">Inactivo</option></select></label></div><div class="nav section"><button class="btn primary" type="submit"><i data-lucide="save"></i>Guardar panel</button><a class="btn" href="{{ route('panels.index') }}">Cancelar</a></div></form></section>
@endsection
